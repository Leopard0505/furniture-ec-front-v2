## プロジェクト構造

本プロジェクトは **Feature-based Architecture** を採用しています。

<pre>
.
├── ARCHITECTURE.md
├── README.md
├── src
│   ├── app/              # アプリケーション設定（store など）
│   ├── assets/           # 静的ファイル
│   ├── constants/        # 定数定義
│   ├── features/         # 機能別ディレクトリ
│   │   ├── auth/         # 認証機能
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   ├── api/
│   │   │   └── routes/
│   │   ├── cart/          # カート機能
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   ├── store/
│   │   │   └── routes/
│   │   ├── checkout/      # 購入機能
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   └── routes/
│   │   ├── favorite/      # お気に入り機能
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   ├── store/
│   │   │   └── routes/
│   │   ├── items/         # 商品機能
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   └── routes/
│   │   ├── orders/        # 注文履歴機能
│   │   │   └── routes/
│   │   ├── shared/        # 共通機能
│   │   │   ├── components/
│   │   │   ├── hooks/
│   │   │   └── utils/
│   │   └── layout/        # レイアウト機能
│   │       └── layouts/
│   ├── interfaces/        # 型定義
│   ├── modules/           # モジュール（axios など）
│   └── test/              # テストユーティリティ
</pre>

## API 仕様

API 仕様をカテゴリごとに整理しています。
API 仕様が変わる場合はここも修正してください。

### 認証関連

| エンドポイント | メソッド | 説明         |
| -------------- | -------- | ------------ |
| /api/login     | POST     | ログイン     |
| /api/logout    | POST     | ログアウト   |
| /api/register  | POST     | ユーザー登録 |

### 商品関連

| エンドポイント      | メソッド | 説明                   |
| ------------------- | -------- | ---------------------- |
| /api/categories     | GET      | カテゴリ一覧           |
| /api/items/ranking  | GET      | 商品一覧（ランキング） |
| /api/items/new      | GET      | 商品一覧（新着）       |
| /api/items/{itemID} | GET      | 商品詳細               |

### カート関連

| エンドポイント     | メソッド | 説明                 |
| ------------------ | -------- | -------------------- |
| /api/cart          | GET      | カート内容           |
| /api/cart          | POST     | 商品をカートに追加   |
| /api/cart/{itemID} | DELETE   | 商品をカートから削除 |

### 購入関連

| エンドポイント | メソッド | 説明     |
| -------------- | -------- | -------- |
| /api/purchase  | POST     | 購入処理 |

### 注文履歴関連（TODO: バックエンド実装後に接続）

| エンドポイント       | メソッド | 説明           |
| -------------------- | -------- | -------------- |
| /api/orders           | GET      | 注文一覧取得   |
| /api/orders/{orderID} | GET      | 注文詳細取得   |

## 画面構成

画面の構成を機能ごとに分類しています。
画面が増えたり減る場合はここも修正してください。

### 認証関連

- `/login` - ログインページ
- `/sign-up` - ユーザー登録ページ

### 商品関連

- `/` - トップページ
- `/items/{itemID}` - 商品詳細ページ

### カート・購入関連

- `/cart` - カートページ
- `/checkout` - 注文手続きページ
- `/purchase/complete` - 注文完了ページ

### マイページ関連

- `/me` - マイページ
- `/me/orders` - 注文履歴
- `/me/orders/{orderID}` - 注文詳細
- `/contact` - お問い合わせ

## パフォーマンス最適化

1. フロントエンド

   - バンドルサイズの最適化
   - 画像の最適化方法
   - レンダリングパフォーマンスの改善

2. モニタリング

   - パフォーマンスメトリクスの収集
   - エラーモニタリング
   - ユーザー行動分析

## React アーキテクチャパターン

React を用いたフロントエンド開発でよく採用されるアーキテクチャパターンとその特徴をまとめています。

### 1. Feature-based Architecture（機能別アーキテクチャ）

機能ごとにディレクトリを分割し、各機能に関連するコンポーネント、フック、状態管理、API などを同じディレクトリに配置するアーキテクチャ。

#### 構造例

```
src/
├── features/
│   ├── cart/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── store/
│   │   └── api/
│   ├── auth/
│   │   ├── components/
│   │   ├── hooks/
│   │   └── store/
│   └── items/
│       ├── components/
│       ├── hooks/
│       └── store/
├── shared/
│   ├── components/
│   ├── hooks/
│   └── utils/
└── app/
    ├── router.tsx
    └── store.ts
```

#### Pros

- **機能の独立性**: 各機能が独立しており、機能単位での開発・保守が容易
- **スケーラビリティ**: 新機能追加時に影響範囲が限定的
- **チーム開発**: 機能単位でチームを分割しやすい
- **コードの局所化**: 関連するコードが一箇所に集約され、理解しやすい
- **リファクタリング**: 機能単位でのリファクタリングが容易

#### Cons

- **コードの重複**: 共通ロジックが各機能に散在する可能性
- **依存関係の管理**: 機能間の依存関係が複雑になる可能性
- **初期設計の重要性**: 機能の境界を適切に定義する必要がある
- **学習コスト**: 新規メンバーが全体像を把握しにくい場合がある

---

### 2. Layer-based Architecture（レイヤー別アーキテクチャ）

技術的な責務（プレゼンテーション層、ビジネスロジック層、データ層など）でディレクトリを分割するアーキテクチャ。

#### 構造例

```
src/
├── components/      # プレゼンテーション層
├── containers/      # コンテナ層（状態管理）
├── hooks/          # ビジネスロジック層
├── services/       # データ層（API 呼び出し）
├── store/          # 状態管理
├── utils/          # ユーティリティ
└── types/          # 型定義
```

#### Pros

- **責務の明確化**: 各レイヤーの責務が明確で、理解しやすい
- **再利用性**: 共通コンポーネントやフックの再利用が容易
- **テスト容易性**: 各レイヤーを独立してテストできる
- **既存パターン**: 従来の MVC や MVP パターンに近い

#### Cons

- **横断的関心事**: 機能が複数レイヤーにまたがり、変更時の影響範囲が広い
- **スケーラビリティ**: プロジェクトが大きくなると、各レイヤーが肥大化
- **ファイル検索**: 関連するファイルを探すのが困難
- **機能追加**: 新機能追加時に複数のレイヤーを変更する必要がある

---

### 3. Component-based Architecture（コンポーネントベース）

コンポーネントを中心に設計し、各コンポーネントが自己完結型の構造を持つアーキテクチャ。

#### 構造例

```
src/
├── components/
│   ├── Button/
│   │   ├── Button.tsx
│   │   ├── Button.module.scss
│   │   ├── Button.test.tsx
│   │   └── Button.stories.tsx
│   └── ItemCard/
│       ├── ItemCard.tsx
│       ├── ItemCard.module.scss
│       └── ItemCard.type.ts
├── pages/
├── hooks/
└── utils/
```

#### Pros

- **コンポーネントの独立性**: 各コンポーネントが独立しており、再利用しやすい
- **保守性**: コンポーネント単位での修正が容易
- **テスト容易性**: コンポーネント単位でテストできる
- **Storybook との親和性**: コンポーネント単位でストーリーを作成しやすい

#### Cons

- **状態管理の複雑化**: グローバル状態の管理が複雑になる可能性
- **プロップスドリリング**: 深い階層でプロップスを渡す必要がある場合がある
- **ビジネスロジックの配置**: ロジックをどこに配置するか判断が難しい

---

### 4. Atomic Design

UI コンポーネントを Atoms（原子）、Molecules（分子）、Organisms（有機体）、Templates（テンプレート）、Pages（ページ）の 5 段階に分類するアーキテクチャ。

#### 構造例

```
src/
├── components/
│   ├── atoms/          # 最小単位（Button, Input など）
│   ├── molecules/      # 複数の atoms の組み合わせ（SearchField など）
│   ├── organisms/      # 複数の molecules の組み合わせ（Header など）
│   ├── templates/      # ページレイアウト
│   └── pages/          # 実際のページ
├── hooks/
└── utils/
```

#### Pros

- **デザインシステムとの親和性**: デザインシステムの構築に適している
- **再利用性**: 小さなコンポーネントから大きなコンポーネントを構築
- **一貫性**: UI の一貫性を保ちやすい
- **段階的構築**: 小さな単位から段階的に構築できる

#### Cons

- **分類の曖昧さ**: コンポーネントの分類が主観的になる可能性
- **過度な抽象化**: 小さなプロジェクトでは過剰な可能性
- **ファイル検索**: どの階層に属するか判断が難しい場合がある

---

### 5. Domain-driven Design (DDD)

ビジネスドメインを中心に設計し、ドメインごとにディレクトリを分割するアーキテクチャ。

#### 構造例

```
src/
├── domains/
│   ├── cart/
│   │   ├── entities/
│   │   ├── useCases/
│   │   ├── repositories/
│   │   └── components/
│   ├── items/
│   │   ├── entities/
│   │   ├── useCases/
│   │   └── repositories/
│   └── auth/
│       ├── entities/
│       ├── useCases/
│       └── repositories/
└── shared/
```

#### Pros

- **ビジネスロジックの明確化**: ドメインロジックが明確に分離される
- **ドメインエキスパートとの協業**: ビジネス側と技術側のコミュニケーションが容易
- **保守性**: ドメイン単位での変更が容易
- **テスト容易性**: ドメインロジックを独立してテストできる

#### Cons

- **複雑性**: 小規模プロジェクトでは過剰な可能性
- **学習コスト**: DDD の概念を理解する必要がある
- **初期設計の重要性**: ドメインの境界を適切に定義する必要がある

---

### 6. Clean Architecture

依存関係の方向を制御し、ビジネスロジックを中心に配置するアーキテクチャ。

#### 構造例

```
src/
├── domain/           # エンティティ、ビジネスロジック
├── application/      # ユースケース
├── infrastructure/   # 外部依存（API、ストレージ）
└── presentation/    # UI コンポーネント
```

#### Pros

- **依存関係の制御**: 依存関係が一方向で、テストしやすい
- **フレームワーク非依存**: フレームワークに依存しないビジネスロジック
- **保守性**: 長期的な保守性が高い
- **テスタビリティ**: 各層を独立してテストできる

#### Cons

- **複雑性**: 小規模プロジェクトでは過剰な可能性
- **ボイラープレート**: 多くの抽象化レイヤーが必要
- **学習コスト**: アーキテクチャの理解に時間がかかる
- **開発速度**: 初期開発速度が遅くなる可能性

---

### 7. Modular Architecture（モジュール型）

機能をモジュールとして独立させ、各モジュールが自己完結型の構造を持つアーキテクチャ。

#### 構造例

```
src/
├── modules/
│   ├── cart/
│   │   ├── index.ts
│   │   ├── Cart.tsx
│   │   ├── cart.store.ts
│   │   └── cart.api.ts
│   ├── items/
│   │   ├── index.ts
│   │   ├── ItemList.tsx
│   │   └── item.store.ts
│   └── auth/
│       ├── index.ts
│       └── Login.tsx
└── app/
```

#### Pros

- **モジュールの独立性**: 各モジュールが独立しており、再利用しやすい
- **動的インポート**: モジュール単位でコード分割が容易
- **チーム開発**: モジュール単位でチームを分割しやすい
- **バージョン管理**: モジュール単位でバージョン管理できる可能性

#### Cons

- **モジュール間の依存**: モジュール間の依存関係が複雑になる可能性
- **共通コードの管理**: 共通コードをどこに配置するか判断が難しい
- **初期設計の重要性**: モジュールの境界を適切に定義する必要がある

---

## 本プロジェクトで採用しているアーキテクチャ

本プロジェクトでは、**Feature-based Architecture と Component-based Architecture のハイブリッド**を採用しています。

### 採用理由

1. **機能別の独立性**: 機能単位での開発・保守が容易
2. **コンポーネントの再利用性**: 共通コンポーネントを `components/` に配置
3. **スケーラビリティ**: 新機能追加時の影響範囲が限定的
4. **チーム開発**: 機能単位でチームを分割しやすい

### 現在の構造

```
src/
├── components/      # 共通コンポーネント（Component-based）
├── routes/         # ページコンポーネント（Feature-based の要素）
├── hooks/          # カスタムフック（機能別に分類可能）
├── stores/         # 状態管理（機能別に分類可能）
├── api/            # API サービス
└── constants/      # 定数定義
```

### 今後の改善案

- `features/` ディレクトリの導入を検討（機能が増えた場合）
- `shared/` ディレクトリの導入を検討（共通コードの明確化）
