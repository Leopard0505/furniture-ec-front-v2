---
description: React エラーハンドリング
alwaysApply: true
---

# エラーハンドリング

エラーが発生する可能性がある処理は適切にハンドリング。

```tsx
try {
  const data = await fetchData();
  setData(data);
} catch (error) {
  console.error("Failed to fetch data:", error);
  setError("データの取得に失敗しました");
}
```
