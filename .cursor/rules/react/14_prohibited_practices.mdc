---
description: React 禁止事項
alwaysApply: true
---

# 禁止事項

- ❌ クラスコンポーネントの使用
- ❌ `any`型の使用（やむを得ない場合を除く）
- ❌ 条件分岐内でのフック呼び出し
- ❌ 不完全な依存配列
- ❌ インデックスを key に使用（一意の ID を使用）
- ❌ コンポーネントから直接`useDispatch`/`useSelector`を使用（カスタムフック経由）
